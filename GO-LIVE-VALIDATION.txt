JARINGANKU PHASE 16 FULL V3 — VPS GO-LIVE PILOT BUNDLE VALIDATION
Date: 2026-08-15

Scope
-----
This bundle is intended for VPS staging/pilot production acceptance.
The application metadata remains 1.2.0-dev / development because Phase 16 FULL V3 has not yet been promoted to a stable mass-production release.

Static validation performed in artifact environment
--------------------------------------------------
- PHP syntax: PASS — 234 PHP files under app/bootstrap/config/database/routes.
- TypeScript/TSX syntax transpile: PASS — 45 files including vite.config.ts.
- JSON parse: PASS — 4 files.
- Docker Compose YAML parse: PASS — docker-compose.yml and docker-compose.prod.yml.
- Bash syntax: PASS — 20 scripts across scripts/, ops/, and docker/*/*.sh.
- Native Nginx syntax: PASS for docker/nginx/default.conf using local nginx -t harness.
- Production PostgreSQL host exposure: PASS — no published 5432 mapping.
- Production Redis host exposure: PASS — no published 6379 mapping.
- Production web bind: PASS — Docker Nginx binds to APP_BIND default 127.0.0.1.
- RADIUS firewall helper: PASS — DOCKER-USER/JARINGANKU-RADIUS whitelist tooling included.
- Destructive production down -v command: PASS — not present in executable shell deployment scripts.
- Secret build-context protection: PASS — .env.production and secrets directory are excluded by .dockerignore.
- Runtime .env / .env.production packaged: PASS — not included.
- Real generated secret files packaged: PASS — not included; only *.txt.example templates.
- Manual PDF included: PASS.

Production deployment tooling included
--------------------------------------
- Ubuntu bootstrap for Docker Engine/Compose, Nginx host and Certbot.
- Production .env + secret generation.
- Docker Compose production stack.
- HTTPS reverse proxy and Let's Encrypt setup.
- RADIUS source-IP/CIDR firewall helper.
- Phase 12-16 preflight, Phase 15 strict security audit, and Phase 16 transactional smoke via existing prod-up/prod-final-check.
- PostgreSQL backup with SHA-256 and guarded restore.
- VPS pilot acceptance script.

Important limitations / acceptance requirements
-----------------------------------------------
- Docker daemon is not available in the artifact build environment; production Docker image build/runtime was NOT executed here.
- FreeRADIUS against a real MikroTik/NAS was NOT tested here.
- HTTPS/Certbot against the user's real domain was NOT tested here.
- Backup restore must be tested on non-production before relying on it for disaster recovery.
- Composer/npm lockfiles are not present in the inherited Phase 16 source. Dependency versions are constrained by semver but the first VPS image build can resolve newer compatible packages. This is acceptable for pilot acceptance but should be locked before stable mass-production promotion.
- Hotspot/voucher/outlet/reseller-balance parity remains outside Phase 16 scope.

Required VPS acceptance before mass onboarding
----------------------------------------------
1. All containers running and health endpoints PASS.
2. HTTPS certificate valid.
3. PostgreSQL/Redis not public.
4. RADIUS 1812/1813 restricted to trusted NAS/VPN CIDRs.
5. One real MikroTik passes RADIUS auth + accounting + CoA/Disconnect path.
6. 3-10 pilot customers pass service activation, billing, overdue suspension and payment reactivation.
7. Customer, Mitra and Inventory portals login and tenant isolation tested.
8. Custom QRIS/logo uploads tested and retained across container restart.
9. Roles/permissions tested with non-admin accounts.
10. Manual backup created; checksum verified; restore tested in non-production.

Expected server-side marker after ops/05-final-acceptance.sh:
  [OK] JARINGANKU PHASE 16 VPS PILOT ACCEPTANCE PASSED
