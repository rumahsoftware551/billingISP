import React,{useState} from 'react';
import {Head,router} from '@inertiajs/react';
import Layout from '../../components/Layout';
import {CheckCircle2,ExternalLink,Image,Search,ShieldCheck,XCircle} from 'lucide-react';
const rp=(v:any)=>'Rp'+Number(v||0).toLocaleString('id-ID');
export default function ManualPayments({proofs,filters}:any){const [status,setStatus]=useState(filters?.status||'pending');return <Layout><Head title="Bukti Pembayaran"/><div className="space-y-6"><div className="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><h1 className="text-2xl font-black">Verifikasi Bukti Pembayaran</h1><p className="mt-1 text-sm text-slate-500">Pembayaran manual dari Customer Portal belum memengaruhi invoice sampai bukti disetujui.</p></div><div className="flex gap-2"><select value={status} onChange={e=>{setStatus(e.target.value);router.get('/billing/manual-payments',{status:e.target.value},{preserveState:true,replace:true})}} className="rounded-xl border border-slate-300 bg-white px-3 py-2 text-sm"><option value="pending">Menunggu Review</option><option value="approved">Disetujui</option><option value="rejected">Ditolak</option><option value="">Semua Status</option></select></div></div><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="min-w-[1050px] w-full text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th className="px-4 py-3">Pelanggan</th><th className="px-4 py-3">Invoice</th><th className="px-4 py-3">Metode</th><th className="px-4 py-3">Nominal</th><th className="px-4 py-3">Bukti</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Aksi</th></tr></thead><tbody>{proofs.data.map((p:any)=><ProofRow key={p.id} proof={p}/>)}</tbody></table></div>{!proofs.data.length&&<div className="p-10 text-center text-sm text-slate-400">Tidak ada bukti pembayaran pada filter ini.</div>}<div className="flex items-center justify-between border-t border-slate-200 p-4 text-sm"><button disabled={!proofs.prev_page_url} className="disabled:opacity-30" onClick={()=>proofs.prev_page_url&&router.visit(proofs.prev_page_url)}>← Sebelumnya</button><span className="text-slate-500">Halaman {proofs.current_page}/{proofs.last_page}</span><button disabled={!proofs.next_page_url} className="disabled:opacity-30" onClick={()=>proofs.next_page_url&&router.visit(proofs.next_page_url)}>Berikutnya →</button></div></section></div></Layout>}
function ProofRow({proof}:any){const [note,setNote]=useState('');const review=(action:'approve'|'reject')=>{if(action==='approve'&&!confirm(`Setujui pembayaran ${rp(proof.amount)} untuk ${proof.invoice?.invoice_number}?`))return;if(action==='reject'&&!confirm('Tolak bukti pembayaran ini?'))return;router.put(`/billing/manual-payments/${proof.id}`,{action,review_note:note},{preserveScroll:true})};return <tr className="border-t border-slate-100 align-top"><td className="px-4 py-4"><div className="font-semibold">{proof.customer?.name}</div><div className="text-xs text-slate-500">{proof.customer?.customer_number} · {proof.customer?.phone||'-'}</div></td><td className="px-4 py-4"><div className="font-semibold">{proof.invoice?.invoice_number}</div><div className="text-xs text-slate-500">Sisa {rp(proof.invoice?.balance_due)}</div></td><td className="px-4 py-4"><div>{proof.method?.name}</div><div className="text-xs text-slate-500">{proof.method?.account_name} {proof.method?.account_number}</div></td><td className="px-4 py-4 font-bold">{rp(proof.amount)}<div className="text-xs font-normal text-slate-500">{proof.payer_name||'-'} · {proof.reference||'tanpa referensi'}</div></td><td className="px-4 py-4"><a href={`/billing/manual-payments/${proof.id}/file`} target="_blank" className="inline-flex items-center gap-1 rounded-lg border border-slate-200 px-2 py-1.5 text-xs font-semibold text-[#0f6cbd]"><Image size={14}/> Lihat Bukti <ExternalLink size={12}/></a></td><td className="px-4 py-4"><span className={`rounded-full px-2 py-1 text-[10px] font-bold uppercase ${proof.status==='approved'?'bg-emerald-50 text-emerald-700':proof.status==='rejected'?'bg-rose-50 text-rose-700':'bg-amber-50 text-amber-700'}`}>{proof.status}</span>{proof.payment?.payment_number&&<div className="mt-1 text-xs text-emerald-700">{proof.payment.payment_number}</div>}</td><td className="px-4 py-4">{proof.status==='pending'?<div className="w-64"><input value={note} onChange={e=>setNote(e.target.value)} placeholder="Catatan review (opsional)" className="w-full rounded-lg border border-slate-300 px-2 py-1.5 text-xs"/><div className="mt-2 flex gap-2"><button onClick={()=>review('approve')} className="inline-flex items-center gap-1 rounded-lg bg-emerald-600 px-2 py-1.5 text-xs font-semibold text-white"><CheckCircle2 size={14}/> Setujui</button><button onClick={()=>review('reject')} className="inline-flex items-center gap-1 rounded-lg bg-rose-50 px-2 py-1.5 text-xs font-semibold text-rose-700"><XCircle size={14}/> Tolak</button></div></div>:<div className="text-xs text-slate-500">{proof.review_note||'Sudah direview'}<div>{proof.reviewer?.name||''}</div></div>}</td></tr>}
