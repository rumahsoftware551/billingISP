import React from 'react';
import {Link, router} from '@inertiajs/react';
const badge=(s:string)=>s==='pass'||s==='passed'?'bg-emerald-50 text-emerald-700':s==='warn'?'bg-amber-50 text-amber-300':'bg-rose-50 text-rose-700';
export default function Release({release,runs,findings}:any){return <div className="min-h-screen bg-slate-50 text-slate-900"><div className="mx-auto max-w-7xl p-6"><div className="flex flex-wrap items-center justify-between gap-3"><div><div className="text-xs uppercase tracking-[.25em] text-blue-700">Jaringanku Release Center</div><h1 className="text-3xl font-black">v{release.version} · {release.channel}</h1><p className="text-slate-500">Final acceptance, security audit, isolation, and reconciliation.</p></div><div className="flex gap-2"><Link href="/platform" className="rounded-lg bg-slate-800 px-4 py-2">Platform</Link><button onClick={()=>router.post('/platform/release/audit',{strict_production:false},{preserveScroll:true})} className="rounded-lg bg-sky-600 px-4 py-2 font-semibold">Run Audit</button><button onClick={()=>router.post('/platform/release/audit',{strict_production:true},{preserveScroll:true})} className="rounded-lg bg-violet-700 px-4 py-2 font-semibold">Strict Production Audit</button></div></div><div className="mt-6 grid md:grid-cols-4 gap-3"><Card label="Version" value={release.version}/><Card label="Channel" value={release.channel}/><Card label="Environment" value={release.environment}/><Card label="Acceptance Runs" value={String(runs.length)}/></div><section className="mt-6 rounded-xl border border-slate-200 bg-white p-5"><h2 className="font-semibold">Acceptance History</h2><div className="mt-3 overflow-auto"><table className="w-full text-sm"><thead className="text-left text-slate-500"><tr><th className="py-2">Run</th><th>Status</th><th>Checks</th><th>Environment</th><th>Executed</th></tr></thead><tbody>{runs.map((r:any)=><tr key={r.id} className="border-t border-slate-200"><td className="py-2"><div className="font-mono text-xs">{r.run_uuid}</div><div className="text-xs text-slate-500">v{r.version}</div></td><td><span className={`rounded px-2 py-1 text-xs ${badge(r.status)}`}>{r.status}</span></td><td>{r.checks_passed} pass · {r.checks_warning} warn · {r.checks_failed} fail</td><td>{r.environment}</td><td className="text-xs text-slate-500">{r.user?.email||'CLI/system'}<br/>{r.completed_at||r.created_at}</td></tr>)}</tbody></table></div></section><section className="mt-6 rounded-xl border border-slate-200 bg-white p-5"><h2 className="font-semibold">Latest Security & Integrity Findings</h2><div className="mt-3 space-y-2">{findings.map((f:any)=><div key={f.id} className="rounded-lg border border-slate-200 p-3"><div className="flex flex-wrap items-center gap-2"><span className={`rounded px-2 py-1 text-[10px] uppercase ${badge(f.status)}`}>{f.status}</span><span className="text-xs text-slate-500">{f.category} · {f.severity}</span><b>{f.title}</b></div><div className="mt-1 text-xs text-slate-500">{f.check_key} · {f.detail||'-'}</div>{f.remediation&&<div className="mt-1 text-xs text-amber-300">Remediation: {f.remediation}</div>}</div>)}{findings.length===0&&<p className="text-slate-500">Belum ada audit tersimpan. Jalankan audit pertama.</p>}</div></section></div></div>}
function Card({label,value}:{label:string,value:string}){return <div className="rounded-xl border border-slate-200 bg-white p-4"><div className="text-xs uppercase text-slate-500">{label}</div><div className="mt-1 text-xl font-bold">{value}</div></div>}
