import React from 'react';
import PartnerLayout from '../../components/PartnerLayout';
import {useForm,usePage} from '@inertiajs/react';
const money=(n:number)=>'Rp'+new Intl.NumberFormat('id-ID').format(n||0);
const input='mt-1.5 w-full rounded-xl border border-slate-300 bg-white px-3 py-2.5 text-sm';
export default function Commissions({balance,entries,withdrawals,payoutAccount}:any){const p:any=usePage().props;const slug=p.tenant.slug;const f=useForm({amount:balance,notes:''});return <PartnerLayout tenantSlug={slug}><div className="mb-6"><h1 className="text-2xl font-black">Komisi & Withdrawal</h1><p className="mt-1 text-sm text-slate-500">Pantau ledger komisi dan ajukan pencairan ke rekening mitra.</p></div><div className="grid gap-5 xl:grid-cols-[360px_1fr]"><section className="h-fit rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="text-xs font-bold uppercase tracking-wide text-slate-400">Saldo Komisi Tersedia</div><div className="my-2 text-3xl font-black text-emerald-700">{money(balance)}</div><div className="rounded-xl bg-slate-50 p-3 text-xs text-slate-500"><div className="font-semibold text-slate-700">Rekening payout</div><div className="mt-1">{payoutAccount?.bank||'Belum disetel'} {payoutAccount?.account||''}</div><div>{payoutAccount?.holder||''}</div></div><form onSubmit={e=>{e.preventDefault();f.post(`/mitra/${slug}/commissions/withdrawals`,{preserveScroll:true})}} className="mt-4 space-y-3"><label className="block text-sm font-semibold">Nominal withdrawal<input type="number" min="1000" value={f.data.amount} onChange={e=>f.setData('amount',Number(e.target.value))} className={input}/></label><label className="block text-sm font-semibold">Catatan (opsional)<textarea rows={3} value={f.data.notes} onChange={e=>f.setData('notes',e.target.value)} className={input}/></label><button disabled={f.processing||balance<=0} className="w-full rounded-xl bg-[#0f6cbd] px-4 py-3 font-bold text-white disabled:opacity-50">Ajukan Withdrawal</button></form></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-bold">Ledger Komisi</h2><div className="mt-3 divide-y divide-slate-100">{entries.data.map((e:any)=><div key={e.id} className="flex items-center justify-between gap-3 py-3"><div><div className="font-semibold capitalize">{String(e.entry_type).replaceAll('_',' ')}</div><div className="text-xs text-slate-500">{e.customer?.customer_number||'-'} · {e.customer?.name||'-'} · {e.status}</div></div><div className="font-bold text-emerald-700">{money(e.amount)}</div></div>)}{!entries.data.length&&<div className="py-8 text-center text-sm text-slate-400">Belum ada komisi.</div>}</div><h2 className="mt-7 font-bold">Riwayat Withdrawal</h2><div className="mt-3 divide-y divide-slate-100">{withdrawals.map((w:any)=><div key={w.id} className="flex items-center justify-between gap-3 py-3"><div><div className="font-semibold">{w.withdrawal_number}</div><div className="text-xs uppercase text-slate-500">{w.status}</div></div><div className="font-bold">{money(w.amount)}</div></div>)}{!withdrawals.length&&<div className="py-6 text-sm text-slate-400">Belum ada withdrawal.</div>}</div></section></div></PartnerLayout>}
