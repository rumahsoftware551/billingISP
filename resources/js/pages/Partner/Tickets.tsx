import React from 'react';
import PartnerLayout from '../../components/PartnerLayout';
import {useForm,usePage} from '@inertiajs/react';
const input='mt-1.5 w-full rounded-xl border border-slate-300 bg-white px-3 py-2.5 text-sm';
export default function Tickets({tickets,customers}:any){const p:any=usePage().props;const slug=p.tenant.slug;const f=useForm({customer_id:customers[0]?.id||'',category:'installation',priority:'normal',subject:'',description:''});return <PartnerLayout tenantSlug={slug}><div className="mb-6"><h1 className="text-2xl font-black">Request & Ticket</h1><p className="mt-1 text-sm text-slate-500">Kirim permintaan instalasi, gangguan teknis atau kendala billing ke tim ISP.</p></div><div className="grid gap-5 xl:grid-cols-[380px_1fr]"><form onSubmit={e=>{e.preventDefault();f.post(`/mitra/${slug}/tickets`,{preserveScroll:true,onSuccess:()=>f.reset('subject','description')})}} className="h-fit rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-bold">Buat Request Baru</h2><div className="mt-4 space-y-3"><label className="block text-sm font-semibold">Pelanggan<select value={f.data.customer_id} onChange={e=>f.setData('customer_id',e.target.value)} className={input}>{customers.map((c:any)=><option key={c.id} value={c.id}>{c.customer_number} · {c.name}</option>)}</select></label><div className="grid grid-cols-2 gap-3"><label className="block text-sm font-semibold">Kategori<select value={f.data.category} onChange={e=>f.setData('category',e.target.value)} className={input}><option value="installation">Instalasi</option><option value="technical">Teknis</option><option value="billing">Billing</option><option value="other">Lainnya</option></select></label><label className="block text-sm font-semibold">Prioritas<select value={f.data.priority} onChange={e=>f.setData('priority',e.target.value)} className={input}><option value="low">Rendah</option><option value="normal">Normal</option><option value="high">Tinggi</option><option value="urgent">Urgent</option></select></label></div><label className="block text-sm font-semibold">Judul / Subjek<input value={f.data.subject} onChange={e=>f.setData('subject',e.target.value)} className={input} placeholder="Contoh: Request instalasi pelanggan baru"/></label><label className="block text-sm font-semibold">Detail permintaan<textarea rows={5} value={f.data.description} onChange={e=>f.setData('description',e.target.value)} className={input} placeholder="Jelaskan kebutuhan atau kendala secara lengkap."/></label><button disabled={f.processing||!customers.length} className="w-full rounded-xl bg-[#0f6cbd] px-4 py-3 font-bold text-white disabled:opacity-50">Kirim Request</button>{!customers.length&&<div className="text-xs text-amber-600">Belum ada pelanggan mitra untuk dipilih.</div>}</div></form><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-bold">Riwayat Request</h2><div className="mt-3 divide-y divide-slate-100">{tickets.data.map((t:any)=><div key={t.id} className="py-4"><div className="flex items-start justify-between gap-3"><div><div className="font-bold text-[#0f6cbd]">{t.ticket_number}</div><div className="mt-1 font-semibold">{t.subject}</div><div className="text-xs text-slate-500">{t.customer?.customer_number} · {t.customer?.name} · {t.category}</div></div><div className="text-right"><span className="rounded-full bg-slate-100 px-2 py-1 text-[10px] font-bold uppercase text-slate-600">{t.status}</span><div className="mt-2 text-[10px] uppercase text-slate-400">{t.priority}</div></div></div><p className="mt-2 text-sm leading-6 text-slate-600">{t.description}</p></div>)}{!tickets.data.length&&<div className="py-10 text-center text-sm text-slate-400">Belum ada request/ticket.</div>}</div></section></div></PartnerLayout>}
