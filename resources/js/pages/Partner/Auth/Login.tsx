import React from 'react';
import {useForm} from '@inertiajs/react';
import {UsersRound} from 'lucide-react';
import {AuthShell,authInput} from '../../../components/AuthShell';
export default function Login({portalTenant}:any){const f=useForm({email:'',password:''});return <AuthShell title="Masuk Portal Mitra" subtitle={portalTenant.name} description="Akses pelanggan area mitra, billing, komisi dan request operasional." icon={<UsersRound size={28}/>}><form onSubmit={e=>{e.preventDefault();f.post(`/mitra/${portalTenant.slug}/login`)}} className="space-y-4"><label className="block text-sm font-semibold text-slate-700">Email mitra<input autoFocus autoComplete="username" type="email" value={f.data.email} onChange={e=>f.setData('email',e.target.value)} className={authInput} placeholder="mitra@contoh.com"/></label>{f.errors.email&&<div className="rounded-lg bg-rose-50 px-3 py-2 text-xs font-semibold text-rose-700">{f.errors.email}</div>}<label className="block text-sm font-semibold text-slate-700">Password<input autoComplete="current-password" type="password" value={f.data.password} onChange={e=>f.setData('password',e.target.value)} className={authInput}/></label><button disabled={f.processing} className="w-full rounded-xl bg-[#0f6cbd] px-4 py-3 font-bold text-white disabled:opacity-50">Masuk Portal Mitra</button></form></AuthShell>}
