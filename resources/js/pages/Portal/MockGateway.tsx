import React from 'react';
import { Head, router } from '@inertiajs/react';
import PortalLayout from '../../components/PortalLayout';
export default function PortalMockGateway({portalTenantSlug,transaction}:{portalTenantSlug:string,transaction:any}){return <PortalLayout tenantSlug={portalTenantSlug}><Head title="Mock QRIS"/><div className="mx-auto max-w-lg rounded-2xl border border-sky-900 bg-white/70 p-7 text-center"><div className="text-xs uppercase tracking-widest text-blue-700">Local Payment Simulator</div><h1 className="mt-2 text-2xl font-black">QRIS MOCK</h1><div className="mx-auto my-6 grid h-52 w-52 place-items-center rounded-xl border-8 border-white bg-white text-center text-sm font-black text-slate-950">JARINGANKU<br/>QRIS<br/>LOCAL<br/>{transaction.order_id}</div><div className="text-2xl font-black">Rp{Number(transaction.amount).toLocaleString('id-ID')}</div><div className="mt-1 text-xs text-slate-500">{transaction.invoice?.invoice_number}</div><button onClick={()=>router.post(`/portal/${portalTenantSlug}/gateway/${transaction.id}/mock-settle`)} className="mt-6 w-full rounded-lg bg-emerald-600 px-4 py-3 font-bold hover:bg-emerald-500">Simulasikan Pembayaran Berhasil</button><p className="mt-4 text-xs text-slate-600">Hanya tersedia pada APP_ENV=local. Tidak memproses uang nyata.</p></div></PortalLayout>}
