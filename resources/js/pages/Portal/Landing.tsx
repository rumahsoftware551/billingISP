import React,{useState} from 'react';
import {Head,Link,usePage} from '@inertiajs/react';
import {ArrowRight,Building2} from 'lucide-react';
export default function PortalLanding(){const [slug,setSlug]=useState('');return <PortalChooser title="Customer Portal" value={slug} setValue={setSlug} prefix="portal"/>}
export function PortalChooser({title,value,setValue,prefix}:{title:string,value:string,setValue:(v:string)=>void,prefix:string}){const page:any=usePage().props;const b=page.branding||{};const primary=b.primary_color||'#0f6cbd';return <div className="flex min-h-screen items-center justify-center bg-slate-50 p-5 text-slate-900"><Head title={`${title} · ${b.app_name||'Jaringanku'}`}/><div className="w-full max-w-lg rounded-3xl border border-slate-200 bg-white p-7 shadow-[0_20px_60px_rgba(15,23,42,.08)] sm:p-9"><div className="flex items-center gap-3">{b.logo_url?<img src={b.logo_url} className="h-12 w-12 rounded-xl object-contain"/>:<div className="grid h-12 w-12 place-items-center rounded-xl bg-blue-50" style={{color:primary}}><Building2 size={24}/></div>}<div><div className="font-black">{b.app_name||'Jaringanku'}</div><div className="text-xs font-bold uppercase tracking-[.15em] text-slate-400">{title}</div></div></div><h1 className="mt-8 text-2xl font-black">Masukkan kode ISP</h1><p className="mt-2 text-sm leading-6 text-slate-500">Kode tenant memastikan Anda masuk ke portal ISP yang benar. Gunakan kode yang diberikan administrator.</p><form className="mt-5" onSubmit={e=>{e.preventDefault();if(value.trim())window.location.href=`/${prefix}/${encodeURIComponent(value.trim())}/login`}}><input autoFocus value={value} onChange={e=>setValue(e.target.value)} placeholder="kode-isp" className="w-full rounded-xl border border-slate-300 px-4 py-3 outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100"/><button disabled={!value.trim()} className="mt-3 flex w-full items-center justify-center gap-2 rounded-xl px-4 py-3 font-bold text-white disabled:opacity-50" style={{background:primary}}>Buka {title}<ArrowRight size={17}/></button></form><Link href="/access" className="mt-5 block text-center text-sm font-bold" style={{color:primary}}>Kembali ke Pusat Akses</Link></div></div>}
