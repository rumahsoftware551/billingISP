import React from 'react';
import {useForm} from '@inertiajs/react';
import {UserRound} from 'lucide-react';
import {AuthShell,authInput} from '../../../components/AuthShell';
export default function Login({portalTenant}:any){const f=useForm({identity:'',password:''});return <AuthShell title="Masuk Customer Portal" subtitle={portalTenant.name} description="Lihat layanan, tagihan, pembayaran dan bantuan pelanggan melalui akun yang terdaftar." icon={<UserRound size={28}/>}><form onSubmit={e=>{e.preventDefault();f.post(`/portal/${portalTenant.slug}/login`)}} className="space-y-4"><label className="block text-sm font-semibold text-slate-700">ID Pelanggan / Email<input autoFocus autoComplete="username" value={f.data.identity} onChange={e=>f.setData('identity',e.target.value)} placeholder="Nomor pelanggan atau email" className={authInput}/></label>{f.errors.identity&&<div className="rounded-lg bg-rose-50 px-3 py-2 text-xs font-semibold text-rose-700">{f.errors.identity}</div>}<label className="block text-sm font-semibold text-slate-700">Password<input autoComplete="current-password" type="password" value={f.data.password} onChange={e=>f.setData('password',e.target.value)} className={authInput}/></label><button disabled={f.processing} className="w-full rounded-xl bg-[#0f6cbd] px-4 py-3 font-bold text-white disabled:opacity-50">Masuk Customer Portal</button></form></AuthShell>}
