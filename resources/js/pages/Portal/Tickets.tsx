import React from 'react';
import {Head, useForm} from '@inertiajs/react';
import PortalLayout from '../../components/PortalLayout';
const input='w-full rounded-lg border border-slate-300 bg-slate-50 px-3 py-2 text-sm outline-none focus:border-sky-500';
export default function Tickets({portalTenantSlug,tickets,services}:{portalTenantSlug:string,tickets:any[],services:any[]}){const f=useForm({customer_service_id:'',category:'technical',priority:'normal',subject:'',description:''});return <PortalLayout tenantSlug={portalTenantSlug}><Head title="Bantuan & Ticket"/><div className="grid gap-6 lg:grid-cols-[360px_1fr]"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-lg font-bold">Buat Ticket Bantuan</h2><form className="mt-4 space-y-3" onSubmit={e=>{e.preventDefault();f.post(`/portal/${portalTenantSlug}/tickets`,{preserveScroll:true,onSuccess:()=>f.reset()})}}><select className={input} value={f.data.customer_service_id} onChange={e=>f.setData('customer_service_id',e.target.value)}><option value="">Semua layanan</option>{services.map(s=><option key={s.id} value={s.id}>{s.service_number} · {s.pppoe_username}</option>)}</select><div className="grid grid-cols-2 gap-2"><select className={input} value={f.data.category} onChange={e=>f.setData('category',e.target.value)}><option>technical</option><option>billing</option><option>installation</option><option>complaint</option><option>other</option></select><select className={input} value={f.data.priority} onChange={e=>f.setData('priority',e.target.value)}><option>low</option><option>normal</option><option>high</option><option>urgent</option></select></div><input className={input} placeholder="Subjek" value={f.data.subject} onChange={e=>f.setData('subject',e.target.value)}/><textarea className={input} rows={5} placeholder="Jelaskan kendala Anda" value={f.data.description} onChange={e=>f.setData('description',e.target.value)}/><button className="rounded-lg bg-sky-600 px-4 py-2 text-sm font-semibold">Kirim Ticket</button></form></section><section className="space-y-3">{tickets.map(t=><Ticket key={t.id} t={t} slug={portalTenantSlug}/>)}{!tickets.length&&<div className="rounded-xl border border-slate-200 p-8 text-center text-slate-500">Belum ada ticket.</div>}</section></div></PortalLayout>}
function Ticket({t,slug}:{t:any,slug:string}){const c=useForm({body:''});return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex justify-between gap-3"><div><div className="font-bold text-blue-700">{t.ticket_number}</div><div className="mt-1 font-semibold">{t.subject}</div><div className="text-xs text-slate-500">{t.service?.service_number||'General'} · {t.category} · {t.priority}</div></div><span className="h-fit rounded bg-slate-800 px-2 py-1 text-[10px] uppercase">{t.status}</span></div><p className="mt-3 text-sm text-slate-700">{t.description}</p><div className="mt-4 space-y-2">{(t.comments||[]).map((x:any)=><div key={x.id} className="rounded-lg border border-slate-200 bg-slate-50 p-3 text-sm">{x.body}<div className="mt-1 text-[10px] text-slate-600">{new Date(x.created_at).toLocaleString('id-ID')}</div></div>)}</div><form className="mt-3 flex gap-2" onSubmit={e=>{e.preventDefault();c.post(`/portal/${slug}/tickets/${t.id}/comments`,{preserveScroll:true,onSuccess:()=>c.reset()})}}><input className={input} placeholder="Tulis balasan" value={c.data.body} onChange={e=>c.setData('body',e.target.value)}/><button className="rounded-lg bg-slate-800 px-3 py-2 text-sm">Kirim</button></form></div>}
