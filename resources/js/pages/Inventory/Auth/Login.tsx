import React from 'react';import {useForm,usePage,Link} from '@inertiajs/react';import {Boxes} from 'lucide-react';import {LoginShell} from '../../Partner/Auth/Login';
export default function Login({portalTenant}:any){const page:any=usePage().props;const b=page.branding||{};const f=useForm({email:'',password:''});return <LoginShell title="Portal Inventory" subtitle={portalTenant.name} logo={b.login_logo_url} icon={<Boxes size={24}/>}><form onSubmit={e=>{e.preventDefault();f.post(`/inventory/${portalTenant.slug}/login`)}} className="space-y-4"><label className="block text-sm font-semibold">Email inventory<input type="email" value={f.data.email} onChange={e=>f.setData('email',e.target.value)} className="field" placeholder="inventory@contoh.com"/></label>{f.errors.email&&<div className="text-xs text-rose-600">{f.errors.email}</div>}<label className="block text-sm font-semibold">Password<input type="password" value={f.data.password} onChange={e=>f.setData('password',e.target.value)} className="field"/></label><button disabled={f.processing} className="w-full rounded-xl px-4 py-3 font-bold text-white" style={{background:b.primary_color||'#0f6cbd'}}>Masuk Portal Inventory</button><Link href="/access" className="block text-center text-sm font-semibold" style={{color:b.primary_color||'#0f6cbd'}}>Pusat Akses</Link></form></LoginShell>}
