import React from 'react';import {Head,Link,usePage} from '@inertiajs/react';import {CircleDollarSign,ClipboardList,Home,LogOut,Users} from 'lucide-react';
export default function PartnerLayout({children,tenantSlug}:{children:React.ReactNode,tenantSlug:string}){const page:any=usePage().props;const url=usePage().url;const b=page.branding||{};const role=page.partner?.account?.role||'sales';const nav:any[]=[['Dashboard',`/mitra/${tenantSlug}/dashboard`,Home],['Pelanggan',`/mitra/${tenantSlug}/customers`,Users]];if(['owner','admin','collector'].includes(role))nav.push(['Billing',`/mitra/${tenantSlug}/billing`,CircleDollarSign]);if(['owner','admin'].includes(role))nav.push(['Komisi',`/mitra/${tenantSlug}/commissions`,CircleDollarSign]);nav.push(['Operasional',`/mitra/${tenantSlug}/tickets`,ClipboardList]);return <div className="min-h-screen bg-[#f5f7fb] text-slate-900"><Head>{b.favicon_url&&<link rel="icon" href={b.favicon_url}/>}</Head><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex max-w-7xl flex-col gap-3 px-4 py-4 md:flex-row md:items-center md:justify-between"><div className="flex items-center gap-3">{b.logo_url?<img src={b.logo_url} className="h-10 w-10 object-contain"/>:<div className="font-black text-[#0f6cbd]">{b.app_name||'JARINGANKU'}</div>}<div><div className="font-bold">Portal Mitra</div><div className="text-xs text-slate-500">{page.partner?.entity?.name}</div></div></div><nav className="flex flex-wrap gap-1">{nav.map(([n,h,I])=><Link key={h} href={h} className={`flex items-center gap-1.5 rounded-lg px-3 py-2 text-sm ${url===h?'bg-blue-50 font-semibold text-[#0f6cbd]':'text-slate-600 hover:bg-slate-50'}`}><I size={15}/>{n}</Link>)}<Link method="post" as="button" href={`/mitra/${tenantSlug}/logout`} className="flex items-center gap-1.5 rounded-lg px-3 py-2 text-sm text-slate-500"><LogOut size={15}/>Keluar</Link></nav></div></header><main className="jk-content mx-auto max-w-7xl px-4 py-7">{page.flash?.success&&<div className="mb-4 rounded-xl border border-emerald-200 bg-emerald-50 p-3 text-sm text-emerald-700">{page.flash.success}</div>}{page.flash?.error&&<div className="mb-4 rounded-xl border border-rose-200 bg-rose-50 p-3 text-sm text-rose-700">{page.flash.error}</div>}{children}</main></div>}
