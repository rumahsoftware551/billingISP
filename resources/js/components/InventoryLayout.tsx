import React from 'react';import {Head,Link,usePage} from '@inertiajs/react';import {Boxes,LogOut} from 'lucide-react';
export default function InventoryLayout({children,tenantSlug}:{children:React.ReactNode,tenantSlug:string}){const page:any=usePage().props;const b=page.branding||{};const inv=page.inventory;return <div className="min-h-screen bg-[#f5f7fb] text-slate-900"><Head>{b.favicon_url&&<link rel="icon" href={b.favicon_url}/>}</Head><header className="sticky top-0 z-30 border-b border-slate-200 bg-white"><div className="mx-auto flex max-w-7xl flex-col gap-3 px-4 py-4 md:flex-row md:items-center md:justify-between"><div className="flex items-center gap-3">{b.logo_url?<img src={b.logo_url} className="h-10 w-10 object-contain"/>:<div className="grid h-10 w-10 place-items-center rounded-lg bg-blue-50 text-[#0f6cbd]"><Boxes size={20}/></div>}<div><div className="font-bold">Portal Inventory</div><div className="text-xs text-slate-500">{inv?.location?.name||page.tenant?.name}</div></div></div><div className="flex items-center gap-2 text-sm"><span className="hidden text-slate-500 sm:inline">{inv?.account?.name} · {inv?.account?.role}</span><Link method="post" as="button" href={`/inventory/${tenantSlug}/logout`} className="flex shrink-0 items-center gap-1 rounded-lg border border-slate-200 px-3 py-2"><LogOut size={14}/>Keluar</Link></div></div></header><main className="jk-content mx-auto max-w-7xl px-4 py-7">{page.flash?.success&&<div className="mb-4 rounded-xl border border-emerald-200 bg-emerald-50 p-3 text-sm text-emerald-700">{page.flash.success}</div>}{page.flash?.error&&<div className="mb-4 rounded-xl border border-rose-200 bg-rose-50 p-3 text-sm text-rose-700">{page.flash.error}</div>}{children}</main></div>}
