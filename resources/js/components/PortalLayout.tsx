import React from 'react';import {Head,Link,usePage} from '@inertiajs/react';import {CircleHelp,Home,LogOut,UserRound} from 'lucide-react';
export default function PortalLayout({children,tenantSlug}:{children:React.ReactNode,tenantSlug:string}){const page:any=usePage().props;const url=usePage().url;const b=page.branding||{};const nav=[['Beranda',`/portal/${tenantSlug}/dashboard`,Home],['Profil',`/portal/${tenantSlug}/profile`,UserRound],['Bantuan',`/portal/${tenantSlug}/tickets`,CircleHelp]];return <div className="min-h-screen bg-[#f5f7fb] text-slate-900" style={{'--portal-primary':b.primary_color||'#0f6cbd'} as React.CSSProperties}><Head>{b.favicon_url&&<link rel="icon" href={b.favicon_url}/>}</Head><header className="sticky top-0 z-20 border-b border-slate-200 bg-white/95 backdrop-blur"><div className="mx-auto flex max-w-6xl flex-col gap-3 px-4 py-4 md:flex-row md:items-center md:justify-between"><div className="flex items-center gap-3">{b.logo_url?<img src={b.logo_url} className="h-10 w-10 rounded-lg object-contain"/>:<div className="text-xl font-black text-[#0f6cbd]">{b.app_name||'JARINGANKU'}</div>}<div><div className="font-bold">{b.app_name||'Jaringanku'}</div><div className="text-[10px] uppercase tracking-[.18em] text-slate-400">Customer Portal</div></div></div><nav className="-mx-1 flex max-w-full items-center gap-1 overflow-x-auto px-1 pb-1 md:pb-0">{nav.map(([n,h,I]:any)=><Link key={h} href={h} className={`flex shrink-0 items-center gap-1.5 rounded-lg px-3 py-2 text-sm font-semibold ${url===h?'bg-blue-50 text-[#0f6cbd]':'text-slate-600 hover:bg-slate-50'}`}><I size={15}/>{n}</Link>)}<Link method="post" as="button" href={`/portal/${tenantSlug}/logout`} className="flex shrink-0 items-center gap-1.5 rounded-lg px-3 py-2 text-sm text-slate-500 hover:bg-slate-50"><LogOut size={15}/>Keluar</Link></nav></div></header><main className="jk-content mx-auto max-w-6xl px-4 py-7"><div className="mb-6 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><div className="font-semibold">{page.portal?.customer?.name||'Pelanggan'}</div><div className="text-xs text-slate-500">{page.portal?.customer?.customer_number} · {page.tenant?.name}</div></div>{page.flash?.success&&<div className="mb-4 rounded-xl border border-emerald-200 bg-emerald-50 p-3 text-sm text-emerald-700">{page.flash.success}</div>}{page.flash?.error&&<div className="mb-4 rounded-xl border border-rose-200 bg-rose-50 p-3 text-sm text-rose-700">{page.flash.error}</div>}{children}</main></div>}
